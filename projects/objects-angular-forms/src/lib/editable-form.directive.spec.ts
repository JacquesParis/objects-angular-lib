import { EditableFormDirective } from './editable-form.directive';

describe('EditableFormDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableFormDirective();
    expect(directive).toBeTruthy();
  });
});
