import { EditableInputContentDirective } from './editable-input-content.directive';

describe('EditableInputContentDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableInputContentDirective();
    expect(directive).toBeTruthy();
  });
});
